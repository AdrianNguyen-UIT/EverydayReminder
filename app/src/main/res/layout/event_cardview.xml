<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    app:cardBackgroundColor="@color/nextCardBackgroundColor"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp"
    android:id="@+id/event_cardview">

    <LinearLayout
        android:id="@+id/CardViewLinearLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/EventTitleText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/roboto"
            android:paddingStart="10dp"
            android:paddingEnd="20dp"
            android:text="Test event text"
            android:textColor="@color/tabUnselectedColor"
            android:textSize="25sp" />

        <TextView
            android:id="@+id/EventTimeText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:layout_marginBottom="5dp"
            android:fontFamily="@font/roboto"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="Test event Time"
            android:gravity="center"
            android:textColor="@color/tabUnselectedColor"
            android:textSize="18sp" />

    </LinearLayout>
</androidx.cardview.widget.CardView>